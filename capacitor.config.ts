import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'movie-gallery-boiler',
    webDir: 'build',
    bundledWebRuntime: false,
};

export default config;
